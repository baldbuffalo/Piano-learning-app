name: simply_piano_clone
description: A Simply Piano_like Flutter Web app
publish_to: 'none'

environment:
  sdk: "3.10.0-158.0.dev" # your requested exact Dart SDK

dependencies:
  flutter:
    sdk: flutter
  # Web piano UI
  piano: 1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0

flutter:
  uses-material-design: true
